import styled from 'styled-components';

export const ShowListWrapper = styled.div`
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: center;
    padding: 1rem;
    background: #f5f5f5;
`;

export const ShowListStyle = styled.ul`
    padding: 0.5rem;
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    justify-content: center
    gap: 1rem;
    width: 100%;
`;
