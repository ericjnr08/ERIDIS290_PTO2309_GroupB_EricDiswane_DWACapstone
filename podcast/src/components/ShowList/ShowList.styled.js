import styled from 'styled-components';

export const ShowListWrapper = styled.div`
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    justify-content: auto;
    padding: 1rem;
    background: #f5f5f5;
`;
